1. Сделать tableRevers не зависимой от глобальной переменной (возвращать таблицу).
2. copyOriginalArray - сделать другое название (отображать структуру).
3. concatToOneArray - как вариант, expandToList.
4. sortArray - не востребована.